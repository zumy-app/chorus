import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.chorus.mobile',
  appName: 'Chorus Mobile',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;